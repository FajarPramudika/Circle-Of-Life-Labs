{
  "name": "dangerous-doppler",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "npm run build && git add dist/ && git commit -m 'Deploy to GH Pages' && git push",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "@studio-freight/lenis": "^1.0.42",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.1",
    "gsap": "^3.15.0",
    "tailwindcss": "^4.3.0"
  }
}
